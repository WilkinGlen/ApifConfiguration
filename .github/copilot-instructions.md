# Copilot Instructions

## Project Guidelines
- Always add braces to if statements in C# code.
- Use private fields without an underscore prefix (e.g., `configuration` not `_configuration`), qualified with `this.` when accessed.
- Always run all tests after making changes in the ApifConfiguration workspace.